function matrixMatch(smallMatrix, largeMatrix) {

    if (smallMatrix.length > largeMatrix.largeMatrix) {
        return false;
    }
    if (smallMatrix[0].length > largeMatrix[0].length) {
        return false;
    }

    for (let i = 0; i < largeMatrix.length; i++) {
        for (let c = 0; c < largeMatrix[i].length; c++) {
            if (largeMatrix[][] === smallMatrix[0][0]) {
                if (matchHelper()) {

                }
            }
        }
    }

    return false;
}